[build]
publish = "public"
command = "hugo --gc --minify --baseURL $URL"

[context.production.environment]
HUGO_VERSION = "0.138.0"
HUGO_ENV = "production"
HUGO_ENABLEGITINFO = "true"

# [context.deploy-preview]
# command = "hugo --buildFuture --buildDrafts --baseURL $URL"

